{
  "id": "ch06-frankenstein-hardware",
  "chapter": 6,
  "title": "Victor Frankenstein Audits an IoT Insulin Pump",
  "context_md": "Victor Frankenstein, Bio-Tech Security Engineer at *Geneva Labs*, is leading the security audit for a next-generation networked insulin pump before FDA submission. The device runs custom firmware on an ARM SoC, communicates over Bluetooth Low Energy with a clinician's tablet, and pulls signed firmware updates from Geneva's CI/CD pipeline.\n\nThree concerns dominate Victor's review. First, the development team built the firmware-signing key into the build server's environment variables — Victor wants the key generation, storage, and signing operations all to happen inside dedicated tamper-resistant hardware. Second, an attacker with brief physical access to the pump should not be able to swap in a malicious firmware image; the device must verify firmware integrity before each boot, and the boot chain itself must be measured so a remote attestation server can detect tampering after the fact. Third, the company's CI/CD pipeline must catch vulnerable open-source dependencies *and* produce a machine-readable inventory that hospitals can ingest into their own vulnerability programs — particularly relevant after the Log4Shell scramble two years ago.\n\nVictor also has to plan for the day this pump model reaches end-of-life, and he must defend against a known class of attacks where a small device is wedged between a USB port and a peripheral to silently capture data.",
  "prompt_md": "Complete Victor's hardware-assurance review by selecting the correct concept for each blank.",
  "body_md": "To remove the firmware-signing key from the build server, Victor will move all signing operations into an **{{key_hardware}}** — a dedicated, tamper-resistant device whose private keys never leave the boundary in plaintext. For per-device measurements and remote attestation of the pump itself, each unit will ship with a **{{platform_module}}** that stores boot-stage hashes in tamper-evident registers.\n\nThe boot process must verify the cryptographic signature on each stage before executing it; this is **{{boot_a}}**. To additionally record what was actually loaded — so a remote server can later confirm the device booted clean firmware — Victor will enable **{{boot_b}}**, which extends measurements into the platform module's PCRs.\n\nIn the CI/CD pipeline, Victor will add **{{sca_tool}}** to flag vulnerable open-source components, and the pipeline will publish a **{{inventory}}** alongside each release so hospitals can match component versions against future CVEs without reverse-engineering the firmware.\n\nTo catch insecure code patterns before the binary is even built, Victor will run **{{static_test}}** against the source tree. To find runtime issues such as authentication bypasses on the BLE interface, the QA team will run **{{dynamic_test}}** against built firmware in a hardware-in-the-loop rig.\n\nFor the day the pump model is retired, Victor will publish a documented **{{eol_plan}}** so hospitals know exactly when security updates will stop and can budget for replacement. Finally, to defend against a malicious device wedged between a USB port and a legitimate peripheral, Victor will warn clinicians in the operations manual about **{{usb_attack}}** and require port covers on the charging dock.",
  "blanks": {
    "key_hardware": {
      "answer": "HSM (Hardware Security Module)",
      "distractors": [
        "TPM (Trusted Platform Module)",
        "USB flash drive",
        "smart card reader",
        "GPU"
      ],
      "explanation": "HSMs are network-attached or PCIe tamper-resistant key vaults purpose-built for high-volume signing operations such as a CI/CD pipeline. TPMs are bound to a single host platform — wrong scale for a build server."
    },
    "platform_module": {
      "answer": "TPM (Trusted Platform Module)",
      "distractors": [
        "HSM (Hardware Security Module)",
        "EEPROM",
        "Self-Encrypting Drive (SED)",
        "MicroSD card"
      ],
      "explanation": "TPMs (or vTPMs in virtualized contexts) provide per-device measured-boot storage (PCRs) and attestation keys — the right primitive for an embedded device, not the build server."
    },
    "boot_a": {
      "answer": "Secure Boot",
      "distractors": [
        "Measured Boot",
        "Cold Boot",
        "Network Boot (PXE)",
        "Recovery Boot"
      ],
      "explanation": "Secure Boot verifies signatures on each boot stage and refuses to execute unsigned/altered code. It is enforcement, not just measurement."
    },
    "boot_b": {
      "answer": "Measured Boot",
      "distractors": [
        "Secure Boot",
        "Warm Boot",
        "Network Boot (PXE)",
        "Recovery Boot"
      ],
      "explanation": "Measured Boot records hashes of each loaded component into TPM PCRs, enabling later remote attestation. Secure Boot enforces; Measured Boot evidences."
    },
    "sca_tool": {
      "answer": "Software Composition Analysis (SCA)",
      "distractors": [
        "SAST",
        "DAST",
        "RASP",
        "Fuzzing"
      ],
      "explanation": "SCA inventories open-source dependencies and matches them against vulnerability databases — exactly the Log4Shell-era control gap."
    },
    "inventory": {
      "answer": "SBOM (Software Bill of Materials)",
      "distractors": [
        "CVE list",
        "CMDB extract",
        "code coverage report",
        "build manifest"
      ],
      "explanation": "An SBOM is the machine-readable component inventory (formats: SPDX, CycloneDX) that downstream operators ingest to track exposure."
    },
    "static_test": {
      "answer": "SAST (Static Application Security Testing)",
      "distractors": [
        "DAST",
        "RASP",
        "IAST",
        "Penetration testing"
      ],
      "explanation": "SAST analyzes source or bytecode without executing it — ideal for catching insecure patterns early in the pipeline."
    },
    "dynamic_test": {
      "answer": "DAST (Dynamic Application Security Testing)",
      "distractors": [
        "SAST",
        "SCA",
        "Linting",
        "Code review"
      ],
      "explanation": "DAST exercises the running system from the outside — the right tool for finding runtime auth/authorization flaws on the BLE attack surface."
    },
    "eol_plan": {
      "answer": "end-of-life (EOL) / supportability schedule",
      "distractors": [
        "marketing roadmap",
        "feature backlog",
        "depreciation table",
        "warranty card"
      ],
      "explanation": "Documented EOL/supportability schedules are required so operators (hospitals, in this case) can plan replacements before security updates cease."
    },
    "usb_attack": {
      "answer": "shimming",
      "distractors": [
        "EMI interference",
        "EMP burst",
        "cold-boot attack",
        "rowhammer"
      ],
      "explanation": "Shimming is the class of attack where a thin malicious device is inserted in line with a port (USB, card reader) to capture data transparently. EMI/EMP and cold-boot/rowhammer target other layers."
    }
  }
}
